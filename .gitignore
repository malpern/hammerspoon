hammerspoon.log
profile-router.log
.DS_Store
venv

# ProfileRouter lives in its own repo (~/local-code/ProfileRouter),
# symlinked into Spoons/ — don't commit the symlink here.
Spoons/ProfileRouter.spoon

# Per-machine route rules for ProfileRouter (dir or symlink -> private dotfiles)
routes
